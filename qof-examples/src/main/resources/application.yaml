server:
  port: 10086

spring:
  application:
    name: qof-examples
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    # 默认使用 Spring Boot 内置 HikariCP，无需指定 type
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://gz-cynosdbmysql-grp-jbtgtckx.sql.tencentcdb.com:21012/mall?serverTimezone=UTC&useUnicode=true&characterEncoding=UTF8&useSSL=false&allowMultiQueries=true
    username: mall
    password: asdL7*U!
    
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
#    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

# 主路径演示：Local + MySQL 元数据 + 内置 HTTP
# 建表见 qof-spring-boot-starter-persistence-mysql/examples/schema.sql
qof:
  web:
    enabled: true
    expose-advice: true
    base-path: /file
  local:
    enable: true
    default-storage-station: c-station
    multiple:
      c-station:
        filepath: ./files/c
      d-station:
        filepath: ./files/d
